# Build outputs
build/

# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcuserstate
*.xcschememanagement.plist
DerivedData/
.DS_Store

# Xcode generated project (regenerated by XcodeGen — not source of truth)
AirCapture/AirCapture.xcodeproj/

# Xcode stub group (not compiled — real sources are in Sources/)
AirCapture/AirCapture/

# Vendor build artifacts (generated by build_lib.sh — not source of truth)
vendor/uxplay/build/

# VS Code
.vscode/

# Documentation (internal only)
prod/

# Distribution scripts (local only — contain credentials and private workflow)
scripts/

# App-specific
*.app
*.dmg
*.ipa

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk
